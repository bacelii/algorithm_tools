"""
Notes on the best python style:

1) 4 spaces per indentation
2) when writing function, 
    -us a hanging indent under the function name
    - have one argument per line
3) max line length of 80 characters
   -> use backslash to make newline
4) Surround top-level function and class definitions with two blank lines.
5) add trailing commas
"""

# Example of hanging indent
def long_function_name(
        var_one, var_two, var_three,
        var_four):
    print(var_one)